"""Nullain Agent SDK — evaluation harness (issue #45).

Not part of the SDK's public API and not shipped in the PyPI distributions
(``nullain-sdk``/``nullain-tools``/``nullain-agentd``); this package is
consumed only by ``make evals`` / ``make evals-live`` and its own test suite
in ``evals/tests``. See ``evals/README.md`` for the full picture.
"""
